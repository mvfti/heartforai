"""
This file is deprecated. Use the root app.py instead.
The application routes are now handled by FastAPI in the root app.py file.

To run the application:
    chainlit run app.py (from the root directory)
"""

# This file is kept for backwards compatibility but is no longer used.
# All routes and handlers are now in /app.py (root directory)
